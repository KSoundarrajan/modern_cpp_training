#include <iostream>
#include <conio.h>
#include "Counter.h"
#include <array>

using namespace std;

//typedef int int_array[];

void Calculate() {
	array<Counter, 20> container;
}

int main()
{
	/*int** array = new int*[10];
	for (int row = 0; row < 10; row++) {
		array[row] = new int[5];
	}

	for (int row = 0; row < 10; row++) {
		for (int col = 0; col < 5; col++) {
			array[row][col] = row * col;
		}
	}

	for (int row = 0; row < 10; row++) {
		for (int col = 0; col < 5; col++) {
			cout << array[row][col] << endl;
		}
	}
	
	for (int row = 0; row < 10; row++) {
		delete [] array[row];
	}

	delete[] array;

	int_array array1 = { 10, 50 };*/

	//int i = 10;
	////int *p = &(i + 2);

	//int* p1 = &i;

	//cout << *p;

	Calculate();

	_getch();
	return 0;
}